import cv2

img = cv2.imread('image.jpg')                     # Read the image
blur = cv2.GaussianBlur(img, (7, 7), 0)           # Apply Gaussian blur

cv2.imshow('Blurred Image', blur)
cv2.waitKey(0)
cv2.destroyAllWindows()
